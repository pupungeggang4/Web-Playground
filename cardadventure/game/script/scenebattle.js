class SceneBattle {
    static loop(game) {
        SceneBattle.render(game)
    }

    static render(game) {
        Render.init(game.ctx)
    }

    static mouseUp(game, key) {

    }

    static keyDown(game, key) {

    }

    static keyUp(game, key) {

    }
}
